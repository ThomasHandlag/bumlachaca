import 'package:flutter/material.dart';

class IconClippedButton extends StatelessWidget {
  const IconClippedButton({super.key});

  @override
  Widget build(BuildContext context) => Material(
          child: InkWell(
        onTap: () {},
      ));
}
